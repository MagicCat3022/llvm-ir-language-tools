# Copy to .env (gitignored) to run the tests that use course homework.
# The directory must contain hw0/ and hw2/. Relative paths are resolved from
# this project's root, and ~ means your home directory.
LLVM_IR_COURSE_DIR=~/Github/cse4100.mum22005
